#!/bin/sh

version=$(git describe --abbrev=0 --tags)
name="xsv2-$version-x86_64-unknown-linux-gnu"

./scripts/build-release
github-release upload --user faradayio --repo xsv2 --tag $version \
  --name "$name.tar.gz" \
  --file "./builds/$name.tar.gz"
